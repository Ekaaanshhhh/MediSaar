{
  "name": "medisaar-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web-app",
    "build": "npm run build --workspace=web-app",
    "start": "npm run start --workspace=web-app",
    "lint": "npm run lint --workspace=web-app"
  }
}
