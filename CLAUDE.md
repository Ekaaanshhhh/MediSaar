@AGENTS.md
@DESIGN.md
## Required reading order
1. DESIGN.md — always read first
2. Current file's directory README if present
3. Existing components in /components/editorial (mirror their patterns)

## Never do
- Add a new design pattern without checking DESIGN.md first
- Use shadcn defaults without applying MediSaar overrides
- Skip the compliance check at the end of a component